// for testing constants
const fs = require('fs');

const {appendFile} = require("./appendFile");
const {access} = require("./access");


// appendFile ("./src/test.txt", "   1abcd")
//  .then ((msg) => console.log(msg))
//  .catch ((err) => console.log (err))

//  access ("./test.txt", fs.constants.R_OK)
// .then ((msg) => console.log(msg))
// // .catch ((err) => console.log (err))
